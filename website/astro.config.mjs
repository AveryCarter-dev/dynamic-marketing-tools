// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://AveryCarter-dev.github.io', // Replace with your GitHub username
  base: '/dynamic-marketing-tools', // Replace with your repo name if different
});
